const COPY={en:{marketplace:'Marketplace',loading:'Loading your pathway…',eyebrow:'Business pathway',country:'Provider country',all:'All countries',providers:'Verified providers',verified:'Current records only',demo:'Staging demonstration',none:'No current verified providers match this pathway and country.',offline:'You are viewing saved information while offline.',open:'Open guide',visit:'Visit provider',failed:'This pathway could not be loaded.'},es:{marketplace:'Mercado',loading:'Cargando su ruta…',eyebrow:'Ruta empresarial',country:'País del proveedor',all:'Todos los países',providers:'Proveedores verificados',verified:'Solo registros vigentes',demo:'Demostración de prueba',none:'No hay proveedores verificados vigentes para esta ruta y país.',offline:'Está viendo información guardada sin conexión.',open:'Abrir guía',visit:'Visitar proveedor',failed:'No se pudo cargar esta ruta.'}};
const params=new URLSearchParams(location.search),id=params.get('id')||'retail',requested=(params.get('locale')||navigator.language||'en'),locale=requested.toLowerCase().startsWith('es')?'es':'en',copy=COPY[locale];
document.documentElement.lang=locale;document.querySelectorAll('[data-i18n]').forEach(el=>el.textContent=copy[el.dataset.i18n]||el.textContent);
const app=document.querySelector('#app');
const safeUrl=(value,external=false)=>{try{const url=new URL(value,location.origin);return (url.origin===location.origin||(!external&&url.protocol==='http:')||url.protocol==='https:')?url.href:null}catch{return null}};
const node=(tag,className,text)=>{const el=document.createElement(tag);if(className)el.className=className;if(text!==undefined)el.textContent=text;return el};
async function getJson(url){const response=await fetch(url,{headers:{accept:'application/json'}});if(!response.ok)throw new Error(String(response.status));return response.json()}
function render(experience){document.title=`${experience.title} · BRASA Business`;app.replaceChildren();const offline=node('p','offline',copy.offline);offline.id='offline';app.append(offline);const intro=node('section','intro'),heading=node('div');heading.append(node('p','eyebrow',copy.eyebrow),node('h1','',experience.title),node('p','lede',experience.disclaimer));const controls=node('div','controls'),label=node('label','',copy.country),select=node('select');label.htmlFor='country';select.id='country';[['',copy.all],['BR','Brasil'],['CR','Costa Rica'],['MX','México'],['US','United States']].forEach(([value,text])=>{const option=node('option','',text);option.value=value;select.append(option)});select.value=params.get('countryCode')||'';controls.append(label,select);intro.append(heading,controls);app.append(intro);const steps=node('ol','steps');experience.steps.forEach(item=>{const li=node('li','step'),h=node('h2','',item.title),p=node('p','',item.description),a=node('a','',copy.open);a.href=safeUrl(item.url)||'/';li.append(h,p,a);steps.append(li)});app.append(steps);const head=node('div','providers-head');head.append(node('h2','',copy.providers),node('span','verified',copy.verified));app.append(head);const list=node('section','providers');list.id='providers';app.append(list,node('p','notice',experience.disclaimer));select.addEventListener('change',()=>loadProviders(experience.id,select.value));loadProviders(experience.id,select.value)}
async function loadProviders(experienceId,country){const list=document.querySelector('#providers');list.replaceChildren(node('p','loading',copy.loading));try{const query=new URLSearchParams({limit:'12'});if(country)query.set('countryCode',country);const result=await getJson(`/api/v1/experiences/${encodeURIComponent(experienceId)}/providers?${query}`);list.replaceChildren();if(!result.data.length){list.append(node('p','empty',copy.none));return}result.data.forEach(item=>{const card=node('article','provider'),h=node('h3','',item.name),meta=node('p','meta',`${item.demonstration?`${copy.demo} · `:''}${item.countryCode}${item.region?` · ${item.region}`:''}`),description=node('p','',item.description),link=node('a','',copy.visit),url=safeUrl(item.websiteUrl,true);if(url&&!item.demonstration){link.href=url;link.rel='noopener noreferrer';}else{link.remove()}card.append(h,meta,description,link);list.append(card)})}catch{list.replaceChildren(node('p','empty',copy.none));if(!navigator.onLine)document.querySelector('#offline')?.classList.add('show')}}
if(!/^[a-z0-9-]{1,80}$/.test(id)){app.replaceChildren(node('p','error',copy.failed))}else{getJson(`/api/v1/experiences/${encodeURIComponent(id)}?locale=${encodeURIComponent(locale)}`).then(result=>render(result.data)).catch(()=>app.replaceChildren(node('p','error',copy.failed)))}
window.addEventListener('offline',()=>document.querySelector('#offline')?.classList.add('show'));if('serviceWorker'in navigator)navigator.serviceWorker.register('/business-sw.js').catch(()=>{});
